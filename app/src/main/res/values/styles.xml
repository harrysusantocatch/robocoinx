<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AlertDialogStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:colorAccent">@color/bottomIcon</item>
        <item name="android:textColor">@color/bottomIcon</item>
        <item name="android:textColorPrimary">@color/bottomIcon</item>
    </style>

    <style name="BlueAccent">
        <item name="colorAccent">@color/midBlue</item>
    </style>

    <style name="GreenAccent">
        <item name="colorAccent">@color/transacGreen</item>
    </style>

    <style name="WhiteAccent">
        <item name="colorAccent">@color/white</item>
    </style>
</resources>